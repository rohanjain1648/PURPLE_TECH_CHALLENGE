FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir requests rich

COPY dashboard/ ./dashboard/

ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1

CMD ["python", "-m", "dashboard.terminal_dashboard", \
     "--store-id", "STORE_BLR_002", \
     "--api-url", "http://api:8000"]
